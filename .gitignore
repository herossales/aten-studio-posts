node_modules/
.env
out/
*.log
.DS_Store

# Fonte de arte do Photoshop: pesado e nao usado em execucao.
# O pipeline le apenas o PNG exportado.
*.psd

# Assets e geracoes dos Reels: ~100MB de video e imagem. Os fixos vivem no
# Release `bases-reels`; o resto e saida descartavel.
Referencia Reels/
